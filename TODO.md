[manager] `manager/State` has `token` -- instead of using `useGlobalToken`

- because one url has one token

# talks

[talks] `Talks` -- chinese
[talks] `Talks` -- english

# docs

[docs] `Docs` -- design
